#include <stdio.h>

int main() {

    int linhas, colunas, ;
    
    printf("Insira do numero de linhas: ");
    scanf("%d", &linhas);
    printf("Insira o numero de colunas: ");
    scanf("%d", &colunas);

    for(int i = 0, i < linhas, i++) {
        for
    }



    return 0;
}